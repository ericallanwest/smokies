# Copy to .env and fill in.  .env is gitignored; this file is not.
#
# Google Maps Routes API key, used only by tools/fetch_drive_times.py to price
# mid-day shuttle drives.  Needs the Routes API enabled on the project.  If the
# key is restricted by HTTP referrer it will 403 from the command line -- an IP
# restriction, or none, is what works here.
GOOGLE_MAPS_API_KEY=

# CARTO basemap API key, used by solver/visualize.py for the CartoDB Light
# basemap. Required since CARTO put raster basemaps behind a key in 2026;
# without it every tile carries an "API KEY REQUIRED" watermark.
# Keys: https://carto.com/basemaps/apikey/
CARTO_BASEMAP_KEY=
